package net.rainbowfurry.PhoenixAPI.builder;

public class ParticleEffectBuilder {

    /*
    * new ParticleEffectBuilder(Particle.FLAME)
    .setAmount(10)
    .setOffset(0.5, 0.5, 0.5)
    .setLocation(player.getLocation())
    .play();
    * */

}
